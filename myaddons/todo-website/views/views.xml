<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="todo-website.list">
      <field name="name">todo-website list</field>
      <field name="model">todo-website.todo-website</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="todo-website.action_window">
      <field name="name">todo-website window</field>
      <field name="res_model">todo-website.todo-website</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="todo-website.action_server">
      <field name="name">todo-website server</field>
      <field name="model_id" ref="model_todo-website_todo-website"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="todo-website" id="todo-website.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="todo-website.menu_1" parent="todo-website.menu_root"/>
    <menuitem name="Menu 2" id="todo-website.menu_2" parent="todo-website.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="todo-website.menu_1_list" parent="todo-website.menu_1"
              action="todo-website.action_window"/>
    <menuitem name="Server to list" id="todo-website" parent="todo-website.menu_2"
              action="todo-website.action_server"/>
    -->
  </data>
</odoo>